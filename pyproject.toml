[project]
name = "pacman"
version = "0.1.0"
description = "Ghosts! More ghosts!"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "mlx",
]

[tool.uv.sources]
mlx = { path = "lib/mlx-2.2-py3-none-any.whl" }
